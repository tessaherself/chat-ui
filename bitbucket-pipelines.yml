image: python:3.13

options:
  docker: true
  size: 2x

definitions:
  caches:
    uv: ~/.cache/uv

  services:
    docker:
      memory: 7000

pipelines:
  branches:
    my-iframe:
      - step:
          name: Build and Push Chat-UI
          caches:
            - docker
          script:
            - curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
            - chmod +x /usr/local/bin/docker-compose
            - export DOCKER_BUILDKIT=0
            - export IMAGE_TAG=$BITBUCKET_COMMIT

            # Write .env.prod with repository variables
            - |
              cat > .env.prod << EOF
              OPENAI_BASE_URL=$OPENAI_BASE_URL
              OPENAI_API_KEY=$OPENAI_API_KEY
              AUTOMATIC_LOGIN=$AUTOMATIC_LOGIN
              OPENID_CONFIG=$OPENID_CONFIG
              USE_USER_TOKEN=$USE_USER_TOKEN
              APP_BASE=$APP_BASE
              ALLOW_IFRAME=$ALLOW_IFRAME
              COOKIE_NAME=$COOKIE_NAME
              MONGODB_URL=$MONGODB_URL
              MONGODB_DB_NAME=$MONGODB_DB_NAME
              MONGODB_DIRECT_CONNECTION=$MONGODB_DIRECT_CONNECTION
              PUBLIC_APP_NAME=$PUBLIC_APP_NAME
              PUBLIC_APP_ASSETS=$PUBLIC_APP_ASSETS
              PUBLIC_APP_DESCRIPTION=$PUBLIC_APP_DESCRIPTION
              PUBLIC_APP_LOGO_URL=$PUBLIC_APP_LOGO_URL
              PUBLIC_APP_LOGO_STYLE=$PUBLIC_APP_LOGO_STYLE
              PUBLIC_APP_LOGO_NAV_URL=$PUBLIC_APP_LOGO_NAV_URL
              PUBLIC_APP_LOGO_NAV_STYLE=$PUBLIC_APP_LOGO_NAV_STYLE
              PUBLIC_SHOW_NAV_LOGO=$PUBLIC_SHOW_NAV_LOGO
              PUBLIC_ORIGIN=$PUBLIC_ORIGIN
              VITE_ALLOWED_HOSTS=$VITE_ALLOWED_HOSTS
              COOKIE_SECURE=true
              COOKIE_SAMESITE=lax
              HF_ORG_ADMIN=xpartners-admins
              EOF

            - echo "Contents of .env.prod:"
            - cat .env.prod

            # Build
            - echo "Building Docker image for chat-ui..."
            - docker build -f Dockerfile-ci -t chat-ui:$IMAGE_TAG -t chat-ui:latest .

            # Push to Azure ACR
            - echo "Authenticating with Azure ACR..."
            - echo $AZURE_ACR_PASSWORD | docker login -u $AZURE_ACR_USERNAME --password-stdin $AZURE_ACR_LOGIN_SERVER
            - echo "Tagging for Azure chat-ui..."
            - docker tag chat-ui:$BITBUCKET_COMMIT $AZURE_ACR_LOGIN_SERVER/chat-ui:$BITBUCKET_COMMIT
            - docker tag chat-ui:$BITBUCKET_COMMIT $AZURE_ACR_LOGIN_SERVER/chat-ui:latest
            - echo "Pushing to Azure ACR..."
            - docker push $AZURE_ACR_LOGIN_SERVER/chat-ui:$BITBUCKET_COMMIT
            - docker push $AZURE_ACR_LOGIN_SERVER/chat-ui:latest
